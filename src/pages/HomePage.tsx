import React from "react";

  //set user data and get user data
  // const { setUser } = useUserContext(); setter for setData from context
  // const { user } = useUserContext(); user data
  // const { data: userData } = useGetUserData(); // get user data from server (hook)

  // useEffect(() => {
  //   setUser(userData);
  // }, [userData, setUser]);



export const HomePage = () => {

  return <>HomePage</>
};
